package mastermind.enums;

import mastermind.interfaces.ISecretType;

// ENUM Lettere che implementa interfaccia generica ISecretType
public enum Lettere implements ISecretType {
    A, B, C, D, E, F
}
